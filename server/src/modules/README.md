Phase-based migration landing zones:

- `health`: public runtime health endpoint.
- `system`: migration metadata and system-level diagnostics.
- `auth`: next phase for authentication and token flow.
- `users`: next phase for user domain services and controllers.
- `settings`: platform configuration module.
- `files`: file upload and storage adapters.
- `notifications`: notification orchestration and delivery.
